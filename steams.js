const fs = require('fs')

const readStream = fs.createReadStream('./docs/blog3.txt') // or.. 
// const readStream = fs.createReadStream('./docs/blog3.txt', {encoding: 'utf8'})

// on is a listener
readStream.on('data', (chunk) => {
    console.log('--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  new chunk --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------');
    console.log(chunk);
    // console.log(chunk.toString());
})